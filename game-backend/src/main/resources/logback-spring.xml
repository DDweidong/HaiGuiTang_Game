<?xml version="1.0" encoding="UTF-8"?>
<!-- 日志规范: 统一走 SLF4J + logback，禁止代码中使用 System.out 打印 -->
<configuration>
    <!-- 控制台输出，带线程名与类名，便于排查并发问题 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <springProfile name="dev">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <!-- 开发环境打印本项目与 MyBatis SQL 的调试日志 -->
        <logger name="com.weidong.gamebackend" level="DEBUG"/>
    </springProfile>

    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>
</configuration>
