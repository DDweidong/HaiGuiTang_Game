# 复制本文件为 .env 并填写实际值(已被 git 忽略, 勿提交)
# docker compose 会自动读取同目录 .env

# MySQL root 密码(与本地开发库一致即可, 容器内 MySQL 与 Windows 上的互相独立)
DB_PASSWORD=你的MySQL密码

# 阿里云百炼 API Key
DASH_SCOPE_API_KEY=sk-xxxx

# JWT 签名密钥: HS256 要求至少 32 字节, 生产环境用强随机值
JWT_SECRET=请替换为至少32字节的随机字符串
