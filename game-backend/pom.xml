<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<!-- 稳定版 3.5.x，替代原 3.5.9-SNAPSHOT（快照版本不能用于生产） -->
		<version>3.5.16</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.weidong</groupId>
	<artifactId>game-backend</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>game-backend</name>
	<description>海龟汤 AI 游戏后端</description>

	<properties>
		<java.version>17</java.version>
		<!-- LangChain4j: 核心库 1.20.0 已 GA，Spring 集成官方仍为 beta（1.20.0-beta30 为最新） -->
		<langchain4j.version>1.20.0-beta30</langchain4j.version>
		<!-- MyBatis-Plus 3.5.9: 经典包结构(extension.service/plugins.inner)，文档与面试认知一致；
		     3.5.17 起重构了包结构(IService 移入 spring 包、分页插件重做)，暂不跟进 -->
		<mybatis-plus.version>3.5.9</mybatis-plus.version>
		<!-- SpringDoc 2.8.x 面向 Spring Boot 3.5；3.x 系列面向 Spring Boot 4，
		     会传递引入 spring-boot-webmvc 4.x 与 3.5.16 冲突（conventionErrorViewResolver 重复注册） -->
		<springdoc.version>2.8.17</springdoc.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<!-- 参数校验: @Valid + @NotBlank 等注解，配合全局异常处理返回友好提示 -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<!-- MyBatis-Plus: 提供分页插件与通用 CRUD，替代原生 mybatis starter -->
		<dependency>
			<groupId>com.baomidou</groupId>
			<artifactId>mybatis-plus-spring-boot3-starter</artifactId>
			<version>${mybatis-plus.version}</version>
		</dependency>
		<!-- MP 扩展模块: Page 分页对象与 MybatisPlusInterceptor 拦截器 -->
		<dependency>
			<groupId>com.baomidou</groupId>
			<artifactId>mybatis-plus-extension</artifactId>
			<version>${mybatis-plus.version}</version>
		</dependency>
		<!-- 分页插件 PaginationInnerInterceptor 自 3.5.9 起独立到该模块 -->
		<dependency>
			<groupId>com.baomidou</groupId>
			<artifactId>mybatis-plus-jsqlparser</artifactId>
			<version>${mybatis-plus.version}</version>
		</dependency>
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>
		<!-- Flyway 数据库迁移: 表结构版本化，新环境一键建表 -->
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>
		<!-- Flyway 10+ 对 MySQL 的支持独立成模块 -->
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-mysql</artifactId>
		</dependency>
		<!-- 接入阿里云百炼平台 -->
		<dependency>
			<groupId>dev.langchain4j</groupId>
			<artifactId>langchain4j-community-dashscope-spring-boot-starter</artifactId>
			<version>${langchain4j.version}</version>
		</dependency>
		<!-- Langchain4j 高级功能（AiServices 声明式代理 / 记忆 / 工具调用） -->
		<dependency>
			<groupId>dev.langchain4j</groupId>
			<artifactId>langchain4j-spring-boot-starter</artifactId>
			<version>${langchain4j.version}</version>
		</dependency>
		<!-- SpringDoc 接口文档: /swagger-ui.html 在线查看与调试 -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>${springdoc.version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Lombok: 减少实体/DTO 样板代码；optional 避免传递到依赖方 -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
